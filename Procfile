# Heroku Procfile - defines process types
# For full-stack deployment
web: cd backend && npm start

# Alternative approach - separate dynos
# web: cd frontend && npx serve -s build -p $PORT
# api: cd backend && npm start